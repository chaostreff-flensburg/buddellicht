# Buddellicht p5.js-server
This electron application sends the loaded p5.js sketch as broadcast artnet. The sketch can be edited in runtime and reloads automatically.

## Installation
Clone the repo and run:

```
npm i
npm start
```
